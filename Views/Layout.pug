<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet", href="/css/normalize.css")
        link(rel="stylesheet", href="/css/app.css")
        script(src='https://kit.fontawesome.com/d10383ab02.js' crossorigin='anonymous')
        title #{nombrePagina}
    body
        header.barra
            h1 LemonTasker - Administrador de Proyectos
            a(href="#") Cerrar Sesion
        
        main.contenedor 
            aside.contenedor-proyectos
                .panel
                    a(href="/nuevo-proyecto" class="boton") Nuevo Proyecto
                        span +
                .panel.lista-proyectos
                    h2 Proyectos 
                    ul#proyectos.proyectos
                        if(proyectos.length)
                            each proyecto in proyectos 
                                li
                                    a(href=`/proyectos/${proyecto.url}`)= proyecto.nombre

            block Contenido
            //- pre= vardump(proyectos)


        script(src="/dist/bundle.js")